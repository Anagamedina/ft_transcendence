# Backend core
fastapi[standard]==0.136.1
uvicorn[standard]

# Database
sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.0,<4.0

# Configuration
pydantic-settings>=2.0,<3.0

#Alembic
alembic==1.19.1
SQLAlchemy>=1.4.0

# Auth (issue #26)
# argon2-cffi: hash de contrasenas. Argon2 es la recomendacion actual
#   (ganador del Password Hashing Competition). Se usa directo en vez de
#   passlib, que esta practicamente sin mantenimiento.
# itsdangerous: firma el contenido de la cookie de sesion (ADR 0001).
argon2-cffi>=23.1,<26.0
itsdangerous>=2.1,<3.0

# Testing
pytest>=8.0

#how install requiremenst:
#cd backend
#python3 -m venv .venv
#source .venv/bin/activate
#pip install -r requirements.txt --index-url https://pypi.org/simple --isolated
#python -c "import sqlalchemy; import psycopg; print('ok')"
