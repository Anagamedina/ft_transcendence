# Implementación — Issue 06

1. Leer el contrato de `POST /api/readings` y elegir un sensor del seed.
2. Separar configuración, generador de valores, cliente HTTP y bucle principal.
3. Implementar escenario normal con intervalo y timeout configurables.
4. Añadir logs útiles, manejo de respuestas no-2xx y cancelación limpia.
5. Preparar una interfaz de escenarios sin implementar reglas de alerta.
6. Probar localmente y en Compose contra `http://backend:8000`; confirmar persistencia.
