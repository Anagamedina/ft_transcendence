# Conceptos — Issue 06

HTTP client — 20 min; contrato JSON — 20 min; intervalo configurable — 15 min; escenarios/estrategia — 30 min; timeout, reintentos y logging — 30 min; red Docker por nombre de servicio — 20 min.

La frontera es `simulator → backend API`; nunca `simulator → database`. Un fallo de red debe producir un error observable y no un bucle descontrolado.

