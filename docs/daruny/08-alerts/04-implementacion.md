# Implementación — Issue 08

1. Acordar estados y campos con Ana antes de migrar.
2. Completar modelo, FK a sensor, timestamps, estado válido e índice de consulta.
3. Implementar `create`, `list` por sensor/estado y `update_status`.
4. Establecer `resolved_at` solo al resolver y limpiarlo si el contrato permite reabrir.
5. Probar transiciones válidas, estado inválido, sensor inexistente y actualización repetida.
6. Entregar métodos y ejemplos; mantener reglas de umbral fuera del repository.

