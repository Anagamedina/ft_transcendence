# Conceptos — Issue 04

Repository — 20 min; separación router/service/repository — 20 min; `select`, filtros y `scalars` — 30 min; transacciones y flush — 20 min; índices y paginación — 30 min.

Un repository recibe una `Session`, encapsula consultas y devuelve entidades o resultados claros. No decide reglas de alerta ni respuestas HTTP. Toda lectura debe quedar asociada al sensor correcto y, cuando aplique, a su organización.

