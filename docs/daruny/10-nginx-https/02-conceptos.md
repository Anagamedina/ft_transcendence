# Conceptos — Issue 10

Reverse proxy — 20 min; TLS/HTTPS — 30 min; certificados y secretos — 25 min; `proxy_pass`/headers — 30 min; SPA fallback — 20 min; WebSocket upgrade — 20 min; puertos — 15 min.

HTTPS cifra el trayecto cliente–gateway; el gateway decide rutas. Los certificados no deben versionarse si son reales. `/ws/` solo queda preparado, no implementa negocio.

