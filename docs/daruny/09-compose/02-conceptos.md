# Conceptos — Issue 09

Compose — 30 min; build context/Dockerfile — 25 min; DNS interno — 15 min; `depends_on` y health — 25 min; volumen — 20 min; puertos publicados — 15 min; configuración por entorno — 20 min.

Publica al host solo lo necesario. Entre contenedores se usan nombres de servicio y puertos internos; un volumen persiste datos aunque se recree el contenedor.

